<div class="dropdown-menu">
  <div class="list-group" data-notifications-target="list">
    <% if current_user.notifications.any? %>
      <% current_user.notifications.each do |notice| %>
        <%  notification = notice.to_notification %>
        <%= render "notifications/static_notification", notification: notification %>
      <% end %>
    <% else %>
      <p class="p-3 mb-0 js-no-items">No notifications yet</p>
    <% end %>
  </div>
</div>
