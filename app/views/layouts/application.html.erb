<!DOCTYPE html>
<html>
  <head>
    <title>FreshCollect - join our community and reduce food waste today!</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload', defer: true %>

    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <link rel="manifest" href="<%= asset_path 'manifest.json' %>">
    <%= render 'shared/ios_pwa_links' %>
    <%= favicon_link_tag %>

    <title><%= meta_title %></title>
    <meta name="description" content="<%= meta_description %>">

    <!-- Facebook Open Graph data -->
    <meta property="og:title" content="<%= meta_title %>" />
    <meta property="og:type" content="website" />
    <meta property="og:url" content="<%= request.original_url %>" />
    <meta property="og:image" content="<%= meta_image %>" />
    <meta property="og:description" content="<%= meta_description %>" />
    <meta property="og:site_name" content="<%= meta_title %>" />
  </head>

  <body class="main">
    <%= render 'shared/navbar' unless current_user.present? || devise_controller? %>
    <%= render 'shared/flashes' %>
    <%#= render "notifications/popup_notification",
      notification: Notification.where(type: "PickupNotification").last.to_notification %>

    <div class="flex-grow-1 testk">
      <%= yield %>
    </div>
    <div class="mobile-link">
      <p class="">
      <br>
        This is web app. Please click on the link below to visit the app in mobile view.
      </p>
      <a href="#" onClick="window.open('https://www.fresh-collect.xyz', '' ,'height=736,width=414'); return false;">Open it in a mobile window</a>
      <br>
      <%= image_tag('Iphone_Home_FC_new.png') %>
    </div>
    <%= render 'shared/footer' if current_page?(root_path) %>
    <%= render "shared/mobile_menu" if current_user.present? %>
  </body>
</html>
